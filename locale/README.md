# Translation boundary

Russian is the source language for the public shell. The checked-in English
copy in `src/webapp/content.py` is reviewed project copy, not an automatically
verified machine translation.

This directory is reserved for Django gettext catalogs when UI strings are
extracted. Locale-prefixed URLs and the current typed content map remain
database-free.
